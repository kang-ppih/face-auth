# Face-Auth IdP System - Environment Variables Sample
# このファイルをコピーして .env ファイルを作成してください
# 注意: .env ファイルは .gitignore に含まれており、Gitにコミットされません

# ============================================================================
# AWS Configuration
# ============================================================================

# AWSリージョン
AWS_REGION=ap-northeast-1

# AWSアカウントID（CDKデプロイ用）
CDK_DEFAULT_ACCOUNT=979431736455

# AWSリージョン（CDKデプロイ用）
CDK_DEFAULT_REGION=ap-northeast-1

# ============================================================================
# Lambda Function Configuration
# ============================================================================
# 注意: これらの環境変数はCDKスタックによって自動的にLambda関数に設定されます
# ローカルテスト時のみ必要です

# S3バケット名（デプロイ後に自動生成）
# FACE_AUTH_BUCKET=face-auth-images-979431736455-ap-northeast-1

# DynamoDBテーブル名
CARD_TEMPLATES_TABLE=FaceAuth-CardTemplates
EMPLOYEE_FACES_TABLE=FaceAuth-EmployeeFaces
AUTH_SESSIONS_TABLE=FaceAuth-AuthSessions

# Cognito設定（デプロイ後に設定）
# COGNITO_USER_POOL_ID=ap-northeast-1_XXXXXXXXX
# COGNITO_CLIENT_ID=your-client-id-here

# Rekognition Collection ID
REKOGNITION_COLLECTION_ID=face-auth-employees

# セッションタイムアウト（時間）
SESSION_TIMEOUT_HOURS=8

# ============================================================================
# IP Access Control Configuration
# ============================================================================

# 許可するIPアドレス範囲（CIDR形式、カンマ区切り）
# 例: 1.2.3.4/32,5.6.7.0/24
# 空の場合: すべてのIPを許可（開発環境用）
ALLOWED_IPS=210.128.54.64/27

# ============================================================================
# Frontend CORS Configuration
# ============================================================================

# 許可するフロントエンドオリジン（カンマ区切り）
# 例: https://app.example.com,https://staging.example.com
# 空または*の場合: すべてのオリジンを許可（開発環境用）
# 本番環境では必ず具体的なオリジンを指定してください
FRONTEND_ORIGINS=*

# ============================================================================
# Notes
# ============================================================================

# 1. このファイルをコピーして .env ファイルを作成してください:
#    cp .env.sample .env
#
# 2. .env ファイルに実際の値を設定してください
#
# 3. 機密情報（パスワード、APIキーなど）は絶対にGitにコミットしないでください
#
# 4. 本番環境では、AWS Secrets ManagerまたはParameter Storeの使用を推奨します
#
# 5. CDKデプロイ時は、環境変数が自動的に読み込まれます
#
# 6. Lambda関数の環境変数は、CDKスタックで自動的に設定されます
#    (FACE_AUTH_BUCKET, CARD_TEMPLATES_TABLE, EMPLOYEE_FACES_TABLE, 
#     AUTH_SESSIONS_TABLE, COGNITO_USER_POOL_ID, COGNITO_CLIENT_ID,
#     REKOGNITION_COLLECTION_ID, AD_TIMEOUT, LAMBDA_TIMEOUT, SESSION_TIMEOUT_HOURS)
#
# 7. ローカルテスト時のみ、上記の環境変数を手動で設定する必要があります
